<Window x:Class="SampleTaskWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SampleTaskWpf"
        mc:Ignorable="d"
        Title="Main Window" Height="600" Width="800" MinWidth="500" MinHeight="500" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height= "Auto"/>
            <RowDefinition Height= "*"/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem Header="File">
                <MenuItem Header="Import" IsEnabled="True" Click="MenuItem_Import" />
                <MenuItem Header="Export" IsEnabled="True" Click="MenuItem_Export" />
                <Separator />
                <MenuItem Header="Exit" Click="MenuItem_Exit" />
            </MenuItem>
            <MenuItem Header="Contacts">
                <MenuItem Header="Add contact" Click="MenuItem_AddContact" />
                <MenuItem Header="Clear contacts" Click="MenuItem_ClearContacts" />
            </MenuItem>
            <MenuItem Header="About" Click="MenuItem_About" />
        </Menu>
        

        <TabControl Grid.Row="1">
            <TabItem Header="List">
                <ListBox/>
            </TabItem>

            <TabItem Header="Grid">
                <DataGrid/>
            </TabItem>
        </TabControl>

    </Grid>
</Window>
